export function getProductionDbConfig({ host, port, company }) {
    return {
        host: host,
        user: company.dbuser,
        password: company.dbpass,
        database: company.dbname,
        port: port,
    };
}