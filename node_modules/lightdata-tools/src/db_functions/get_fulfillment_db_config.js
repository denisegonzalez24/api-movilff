export function getFFProductionDbConfig({ host, port, companyId }) {
    return {
        host: host,
        user: `ue${companyId}`,
        password: `78451296_${companyId}`,
        database: `empresa_${companyId}`,
        port: port,
    };
}